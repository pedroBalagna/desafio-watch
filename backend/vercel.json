{
  "version": 2,
  "buildCommand": "pnpm prisma generate && pnpm run build",
  "installCommand": "pnpm install",
  "framework": null,
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
